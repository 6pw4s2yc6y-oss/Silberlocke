{
  "name": "silberlocke",
  "version": "1.0.0",
  "description": "SILBERLOCKE – persönlicher Supplement-Tagesplan (PWA).",
  "type": "module",
  "private": true,
  "scripts": {
    "check:syntax": "node scripts/check-syntax.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "test": "node --test tests/*.test.mjs",
    "ci": "npm run check:syntax && npm run validate:data && npm test"
  },
  "license": "MIT"
}
